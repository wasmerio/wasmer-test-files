This repository contains binary files used by some of Wasmer's tests.
It's separate so that if it gets too large, we can prune commits
or otherwise rewrite history in order to reduce its size,
things that we definitely don't want to do with the main wasmer repository.
